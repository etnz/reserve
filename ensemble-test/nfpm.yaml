# yaml-language-server: $schema=https://nfpm.goreleaser.com/schema.json

name: "ensemble-test"
arch: "all"
platform: "linux"
version: "1.0.0"
maintainer: "${CLUSTER_MAINTAINER}"
description: "Letter of Introduction for accessing La Réserve"
contents: []
depends:
  - letter-of-introduction
